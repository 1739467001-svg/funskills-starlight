# 繁星之夜 · FunSkills 决赛作品星河长廊 —— 项目制作文档

> 一个为「繁星计划 · FunSkills 大赛」全国总决赛打造的、以「繁星 / 恒星 / 星座」为主题的作品展示网站。
> 从一句需求到公网上线、再到点赞评论与后台管理，全程由 Claude(Opus）与用户协作完成。
>
> 文档生成日期：2026-06-03

---

## 一、项目概览

| 项 | 内容 |
|---|---|
| 项目名 | 繁星之夜 · FunSkills 决赛作品星河长廊 |
| 目标 | 让进入决赛的 Skill 作品集中曝光展示，人人可看、可互动 |
| 设计主题 | 深空星河 · 繁星 / 流星 / 星座 / 金色点缀 |
| 作品数 | 38 个（37 位选手，其中王林 2 件作品） |
| 技术栈 | 纯静态前端（HTML/CSS/JS）+ 零依赖 Python 后端（标准库 + SQLite）+ Nginx |
| 上线地址 | GitHub Pages（海外）+ 用户云服务器（国内可访问） |
| 公网地址 | http://43.133.22.250:8088/ ｜ https://1739467001-svg.github.io/funskills-starlight/ |
| 后台 | /admin.html（密钥保护，点赞/评论增删改查） |

---

## 二、需求与对话脉络（时间线）

整个项目是在持续对话中逐步成型的，下面按对话顺序还原每一步的需求与产出：

1. **初始需求** —— 用户提供一文件夹的决赛选手海报，要求做一个有「大轮播 + 繁星/星座元素」的创意展示页，「大胆做、见识 Opus 的威力」。
2. **读图建库** —— 逐张读取 40+ 张海报/PDF，提取真实 Skill 名称、副标题、一句话主张、作者，整理成 37 个作品的数据。
3. **首版网站** —— 实现星空 Canvas、3D Coverflow 大轮播、星座网格、灯箱详情，本地验证通过。
4. **部署上线（GitHub Pages）** —— 用户用设备码授权 GitHub，建公开仓库、推送、开启 Pages，得到永久 HTTPS 链接。
5. **国内访问讨论** —— 说明 GitHub Pages / Vercel / Netlify 在国内不稳定，给出方案建议。
6. **多终端响应式适配** —— 手机 / 平板 / 桌面逐档真机视口验证并修复（导航、统计、轮播、灯箱、横向溢出）。
7. **部署到云服务器** —— 通过 SSH 密钥（用户本机 ssh-copy-id，全程不向 AI 提供密码）部署到 Ubuntu 服务器；因 80/8080 被占用，自动选空闲端口 8088，Nginx 静态托管，不影响原有应用。
8. **二维码海报** —— 用 Python(qrcode+Pillow）生成繁星主题二维码海报，OpenCV 解码校验。
9. **选手署名修正** —— 按用户提供的对照修正 10+ 位选手署名。
10. **作品详情加社群二维码** —— Web3QuantMaster、银砚 等作品详情加入微信群二维码。
11. **数据沉淀（点赞 + 评论）** —— 自建零依赖后端 + SQLite，观众填昵称即可点赞/评论，数据全站共享、卡片角标 + 全场实时总计、自动刷新。
12. **后台数据看板** —— /admin.html 看「谁给谁点赞 / 评论」。
13. **选手联系方式（微信号）** —— 结合权威对照表，为 38 个作品全部补齐选手姓名 + 微信号（详情页绿色微信栏 + 一键复制）。
14. **图文核对与纠错** —— 发现并修复 CBED 配图错放（实为法小蕾「古今穿越」漫画），新增法小蕾为第 38 个作品。
15. **后台升级为增删改查** —— 后台支持评论的新增 / 编辑 / 删除、点赞删除、搜索筛选。
16. **细节打磨** —— 二维码改回 IP、去掉海报上的 IP 文字、缓存版本号治理等。

---

## 三、网站功能

### 1. 首屏（Hero）
- 全屏 Canvas 实时星空：视差闪烁星点、随机流星、星轨光环。
- 渐变发光主标题「繁星之夜」、数字滚动统计（SKILLS / FINALISTS / 赛道 / ∞）。

### 2. 星河长廊（3D 轮播）
- Coverflow 立体轮播，中心放大、两侧倾斜淡出。
- 自动播放 + 拖拽 + 方向键 + 圆点导航；点击中心进入详情。

### 3. 星座图谱（作品网格）
- 5 条赛道（电商出海 / 金融投资 / 效率工具 / 内容创作 / 生活成长）筛选。
- 卡片随滚动逐颗「点亮」（交错淡入）。
- 卡片角标显示 ♥ 赞数 / 💬 评论数；区顶「全场已点亮 N 次心动 · 收到 N 条留言」实时总计。

### 4. 作品详情（灯箱）
- 高清海报 + 赛道 + 标题 + 主张 + 简介 + 标签 + **选手 + 微信号(一键复制)**。
- 部分作品含「全程亮点」「作者社群二维码」。
- **点赞 + 评论**：填昵称即可，无需注册；数据持久共享。
- 上一张 / 下一张 / Esc，移动端上下堆叠可滚动。

### 5. 数据沉淀
- 所有点赞 / 评论写入服务器统一 SQLite，人人可见。
- 45 秒自动刷新 + 切回页面刷新，能看到他人的最新互动。

### 6. 后台管理（/admin.html）
- 密钥保护；统计卡片 + 按作品汇总。
- 评论：新增 / 编辑 / 删除 + 关键词搜索。
- 点赞：删除。

---

## 四、技术架构

```
                          ┌─────────────────────────────┐
   观众浏览器  ──────────▶ │  Nginx  (云服务器 :8088)      │
                          │  ├── 静态站点 (/var/www/...)  │
                          │  └── /api/  反代 ───────────┐ │
                          └────────────────────────────┼─┘
                                                       ▼
                                   Python 后端 (systemd, 127.0.0.1:8090)
                                   标准库 http.server + sqlite3
                                                       │
                                                       ▼
                                   SQLite (/var/lib/funskills/engagement.db)
                                   —— 在 web 根目录之外，不可被下载
```

- **前端**：纯静态，无构建。`index.html` + `assets/style.css` + `assets/app.js` + `assets/data.js`（作品数据）+ `assets/works/`（海报与缩略图）。
- **后端**：`server/api.py`，Python 标准库 + SQLite，零第三方依赖；systemd 服务 `funskills-api`。
- **接口**：
  - `GET /api/stats?slug=&name=` 单作品点赞数/是否已赞/评论列表
  - `GET /api/summary` 各作品计数（首页角标用）
  - `POST /api/like` `{slug,name}` 点赞/取消（同名同作品只算一个）
  - `POST /api/comment` `{slug,name,text}` 评论
  - `GET /api/all?key=` 后台全量数据（需密钥）
  - `POST /api/admin` `{key,action,...}` 后台增删改（del_comment / edit_comment / add_comment / del_like）
- **安全**：评论 XSS 转义、限长、同源 `/api`；后台密钥保护；数据库在 web 根目录之外。

---

## 五、部署与运维

### 两个上线目标（每次更新都要同步）
1. **GitHub Pages**：`git push` 后自动构建（静态镜像，无后端，点赞评论以服务器站点为准）。
2. **云服务器（Ubuntu 24.04，Nginx，端口 8088）**：站点目录 `/var/www/funskills-starlight`（git clone 而来）。

### 标准更新流程
```bash
# 本地
git add -A && git commit -m "说明" && git push origin main
# 服务器
ssh ubuntu@<服务器> 'cd /var/www/funskills-starlight && sudo git pull && sudo systemctl reload nginx'
# 若改了 server/api.py，需额外：sudo systemctl restart funskills-api
```

### 关键约定
- **缓存版本号**：改了 css/js/data 要把 `index.html` / `admin.html` 里的 `?v=N` 同步 +1；换了海报图要把 `app.js` 里的 `VER` +1。
- **SSH 安全**：服务器用密钥免密访问；**禁止使用密码登录**（密码类凭据由用户本人输入，AI 不接触）。
- **端口**：服务器 80 / 8080 被原有应用占用，本站用空闲端口 8088，未触碰原有服务。

---

## 六、目录结构

```
繁星之夜-showcase/
├── index.html                 # 主页面
├── admin.html                 # 后台管理（密钥保护）
├── CLAUDE.md                  # 维护说明（给后续协作者/AI）
├── 繁星之夜-项目制作文档.md      # 本文档
├── 二维码海报.png               # 繁星主题二维码海报（2160×3000）
├── assets/
│   ├── style.css              # 样式
│   ├── app.js                 # 交互（星空/轮播/网格/灯箱/点赞评论/汇总）
│   ├── data.js                # 38 个作品数据（标题/选手/微信/简介/赛道/二维码）
│   ├── works/                 # 海报（+ thumbs/ 缩略图）
│   └── qr/                    # 作品社群二维码
└── server/
    └── api.py                 # 点赞/评论后端（标准库 + SQLite）
```

---

## 七、设计与工程亮点

- **真实数据驱动**：所有作品标题/主张均从选手海报逐张读取提炼，而非占位。
- **不破坏既有环境**：服务器已有容器应用占用 80/443、Node 应用占用 8080，全程未触碰，自动选用空闲端口。
- **凭据安全边界**：GitHub 用设备码授权、服务器用 SSH 密钥，密码/Token 等敏感凭据始终由用户本人输入，AI 不代为登录。
- **零依赖后端**：点赞评论后端仅用 Python 标准库 + SQLite，部署简单、无需 pip/venv，systemd 守护、Nginx 反代。
- **数据沉淀可视化**：互动写入统一数据库，卡片角标 + 全场总计 + 自动刷新，人人可见。
- **全终端响应式**：手机 / 平板 / 桌面真机视口验证，零横向溢出。
- **可维护性**：缓存版本号治理、CLAUDE.md 沉淀维护约定、图文一致性核对。

---

## 八、最终成果

- ✅ 38 个作品，全部含「正确海报 + 选手姓名 + 微信号」。
- ✅ 星空主题展示页，3D 轮播 + 星座筛选 + 灯箱详情。
- ✅ 观众点赞 / 评论（无需注册），数据持久共享、实时可见。
- ✅ 后台数据看板 + 评论增删改查。
- ✅ 双地址上线（GitHub Pages + 国内云服务器），繁星主题二维码海报。

> ✦ 一星一世界，都值得被点亮 ✦
>
> —— 原子公社：AI 时代下人与 Agent 共建的开源学习社区
